{
  "name": "lanka-data-layer",
  "private": true,
  "version": "0.1.0",
  "description": "Open geo-data platform for Sri Lanka: the Lanka Data Layer API and visualization platform",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "foundry": "pnpm --filter @lanka-data-layer/foundry",
    "api": "pnpm --filter @lanka-data-layer/api",
    "web": "pnpm --filter @lanka-data-layer/web"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.0"
  }
}
